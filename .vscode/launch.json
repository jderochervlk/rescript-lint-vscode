{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ReScript Lint",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.mjs"],
      "preLaunchTask": "build"
    }
  ]
}
